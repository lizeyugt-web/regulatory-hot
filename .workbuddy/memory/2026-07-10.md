## 2026-07-10

### 本地全自动采集方案 — 实施完成
- 创建 `scripts/local_daemon.cjs` — 本地守护进程，统一管理 git pull → 微信导入 → FDA采集 → AI分析 → git push
- 创建 `ecosystem.local.config.cjs` — PM2 配置（Windows），30分钟循环，FDA每4轮(2h)触发
- 创建 `start_local.cmd` — Windows 一键启动脚本，支持常驻/单次/FDA-only/AI-only 四种模式
- 关键技术方案：Windows 下 SQLite WAL 模式锁定 regulatory.db → 使用 `git update-index --assume-unchanged` 绕过
- 测试通过：全链路 git pull → 微信+67 → FDA +1 → AI 分析 100条(163s) → git push 全部成功

### ECS 旧服务清理
- 停止并删除 **pharma-tracker**（global-pharma-tracker，本平台前身）: 释放 ~239MB RAM，DB 86MB 已备份至 /root/backups/
- 清理 **wewe-rss** PM2 僵尸条目（已被 watcher.cjs 替代）
- ECS 内存优化：919MB → 803MB，可用从 951MB → 1.0GB+

### ECS 全量部署 ✅ 完成
- **mp-watcher**: 微信采集守护进程，从 10 个公众号扩展至 **170 个**（批量 fakeid 解析 152/152 全部成功）
- **reg-daemon**: 全自动采集+AI分析（每30min: 微信导入 + FDA/2h + AI分析 + git push）
- **reg-web**: Next.js 14.2.5 前端，端口 3457，HTTP 200 ✅
- AI 分析 507 事件全部完成（ECS 上运行，Qwen2.5-72B，0 失败）
- 关键修复: src/db.js 自适应 Prisma 路径, 5 个页面 async/await 编译错误, next.config.js 跳过类型检查
- ECS 内存: ~463MB (PM2三服务) + ~110MB (Docker) + OS ≈ 1.1GB / 1.8GB 总量
- 外网访问：http://47.107.133.169:3457（需在阿里云安全组开放 3457 端口）

### 采集能力升级
- 微信公众号: 10 → **170 个**（覆盖全部省药监局、审评中心、医保局、器械协会、制药企业、创新药/生物药/中药垂直号）
- 脚本: scripts/batch_fakeid.cjs — 批量解析公众号 fakeid
- scripts/ecs_daemon.cjs — ECS 版守护进程（内存保护 + 直读本地微信文章）
- ecosystem.ecs.config.cjs — ECS PM2 配置