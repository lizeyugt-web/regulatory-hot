# 2026-07-09 工作日志

## 微信公众号智能采集改进 (v4.0 第二轮迭代)

**泽神反馈**: 不要阿里云 cron，全部 GitHub Actions 驱动；FDA 和微信拆分独立 workflow；微信只在 0 点全量兜底昨天，其余每 30 分钟增量。

**产出**:
- `scripts/collect_wechat_smart.cjs` — 智能采集脚本（~300行）
  - 运行时自动检测北京时间 → 0:00(全量昨天) / 其他(增量31分钟)
  - 增量模式: 5篇/号 + ID比对去重，~15s 完成
  - 全量模式: 20篇/号 + 昨天时间窗口过滤
  - 修复 beijingNow() 时区bug：改用 UTC 基准计算，跨时区通用
- `.github/workflows/wechat-sync.yml` — 微信独立 workflow，cron `*/30 * * * *`
- `.github/workflows/collect-analyze.yml` — 精简为纯 FDA workflow，cron `0 */2 * * *`
- `ARCHITECTURE.md` — 更新至 v4.0 智能采集
- `scripts/test_wechat_smart.cjs` — 22项完整链路测试，全部通过
- 已成功 push 到 GitHub (main: acb34b9)

**测试结果**: 全部 22 项通过（6大场景：增量/全量模式、时间窗口、文章规范化、合并去重、边界条件、状态追踪）

**待验证**: 需在 GitHub Actions 手动触发 wechat-sync workflow 进行真实微信 API 采集测试。https://github.com/lizeyugt-web/regulatory-hot/actions

**架构决策**: FDA 和微信完全解耦，各自独立采集+AI分析+提交，通过 git pull --rebase 处理冲突。

**效果**: 微信延迟 ≤30min（增量）+ 每日0点全量兜底。不再需要阿里云维护 cron。

## 竞品调研
- 调研 AIHOT/Hacker News/Techmeme/NewsBlur/36氪 5大站点
- AIHOT 最对标：168信源、五维评分+代码算分、无独立后端
- 前端展示 Spec 已固化: docs/specs/SPEC-frontend-display.md
- 数据库写入 Spec 已固化: docs/specs/SPEC-database-write.md

## 数据库后端建设 (方案B: SQLite + Prisma)

### Phase 1: ✅ Prisma + SQLite
- Prisma 7.8 + @prisma/client + better-sqlite3 + @prisma/adapter-libsql
- 4 表: events(44字段) / sources / crawl_logs / ai_analyses
- 适配 Prisma 7 新配置 (prisma.config.ts)

### Phase 2: ✅ 数据迁移
- 439 条 events.json → SQLite
- 12 个信源 → sources 表 (含微信 fakeid)
- 微信/FDA 同表，merge_wechat.cjs 可退役

### Phase 3: ✅ API + 前端切换
- /api/events / [id] / /api/search(FTS5) / /api/stats
- FTS5 全文检索 "FDA" 返回 3 条，工作正常
- events-data.ts 切换为 Prisma 直读数据库
- page.tsx + all/page.tsx 改为 async

### Phase 4: ⏳ 采集脚本改造 (待做)
- collect_fda.cjs / collect_wechat_smart.cjs 直写 SQLite
- analyze.cjs 读 SQLite pending → AI → 写回
- GitHub Actions 更新
