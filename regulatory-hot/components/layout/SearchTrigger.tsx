'use client';

/**
 * 全局命令面板触发器 stub
 *
 * 此文件原本被 SiteSidebar 引用，作为「按 ⌘K 唤起 CommandPalette」的事件桥。
 * 真实触发逻辑已合并到 SiteSidebar 内的 SearchTriggerButton，本组件保留为
 * 兼容占位，避免破坏外部导入。
 */

export function SearchTrigger() {
  return null;
}
